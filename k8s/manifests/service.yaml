apiVersion: v1
kind: Service
metadata:
  name: go-portfolio-service
  labels:
    app: go-portfolio
spec:
  type: ClusterIP
  selector:
    app: go-portfolio
  ports:
  - port: 80
    targetPort: 8080