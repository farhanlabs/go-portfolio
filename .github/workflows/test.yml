name: Test Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Test
        run: |
          echo "Hello from Self Hosted Runner"
          hostname
          whoami
          pwd