name: Test Workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkoutttttttts
        uses: actions/checkout@v4

      - name: Test
        run: |
          echo "GitHub Actions is working!"
          uname -a